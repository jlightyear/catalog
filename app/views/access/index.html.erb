<% @page_title = "Admin Menu" %>

<div class="row vertical-center70">
  <div class="col-md-4 col-md-offset-4">
    <div class="panel panel-info">
      <div class="panel-heading">
        <h3 class="panel-title">Admin Menu</h3>
      </div>
      <div class="list-group">
    		<%= link_to("Administrar catálogo", sections_path, :class => "list-group-item") %>
        <%= link_to("Administrar colores", colors_path, :class => "list-group-item") %>
    		<%= link_to("Ver catálogo", home_index_path, :target => '_blank', :class => "list-group-item") %>
    		<%  if session[:super_admin] %>
          <%= link_to("Administrar usuarios admin", admins_path, :class => "list-group-item") %>
        <% end %>
        <%= link_to("Logout", {:controller => 'access', :action =>'logout'}, :class => "list-group-item") %>
      </div>
    </div>
  </div>
</div>